<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Netzwerk: API-Calls + WebSocket-Sync gegen hotdoc-api.fly.dev. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- GPS: Florianstation-Live-Position-Tracking + Routing zu Einsatzort. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Display wach halten waehrend aktiver Einsatz (KeepAwake-Plugin). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Vibration bei neuem Alarm (haptic feedback). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- FCM-Notifications: ab Android 13 explizit anforderbar. Plugin
         fragt zur Laufzeit nach. Vor Android 13 implizit. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot-Persistenz: App startet nach Tablet-Reboot mit, fuer FCM-
         Empfang ohne dass der Nutzer die App erst manuell oeffnen muss. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground-Service: dauerhaftes Tracking waehrend des Einsatzes
         (Phase 2 — noch nicht implementiert, Permission jetzt schon
         deklariert damit das Update kein Re-Install braucht). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
</manifest>
